stages:
  - test

test:
  stage: test
  script:
    - apk update
    - apk add git
    - pip install -r requirements.txt
    - pytest --junitxml=report.xml
  artifacts:
    when: always
    reports:
      junit: report.xml

