image: "ubuntu"
stages:
  - test
default:
  before_script:
    - sudo apt clean
    - apt-get update -qy
    - apt-get install -y python python3-pip
    #- apk update 
    #- apk add python py-pip
test:
  stage: test
  script:
    - pip install -r requirements.txt
    - python -m pytest

